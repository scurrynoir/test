接力扣5
```
vector<vector<bool>> dp(n,vector<bool>(n,false));
```

### 拆开，由内向外读

1. `vector<bool>(n,false)`
创建一个**一维 vector**，里面有`n`个`bool`元素，**全部初始化为 false**。
也就是一行，n 个格子，全是 false。
2. `vector<vector<bool>> dp(n,  这里放上面那一行 )`
外层再套一层 vector：一共创建 **n 行**，每一行都是上面那个`n个false`的一维 vector。

最终效果：得到一个 **n 行 × n 列** 的二维布尔数组
即：
```
dp[0][0]  dp[0][1]  dp[0][2] ... dp[0][n‑1]
dp[1][0]  dp[1][1]  dp[1][2] ... dp[1][n‑1]
...
dp[n‑1][0] ...                dp[n‑1][n‑1]
```
所有格子初始值全部是`false`。

——————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————

### 含义对应题目

`dp[i][j]` → 代表字符串中，下标从`i`到`j`的子串，**是不是回文**

- `dp[i][j] = true`：`s[i] ~ s[j]`是回文
- `dp[i][j] = false`：不是回文

> 注意：这个二维数组，**只使用上三角区域**，也就是 `i <= j`。
  因为子串起点 i 不能大于终点 j，`i>j`的格子没有意义，不会访问。

———————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————
### 等价理解，类比静态数组
如果是静态数组写法（不能直接用，n 是变量，C++ 不允许）

```
bool dp[n][n];
```

但`n`是运行时得到的字符串长度，不是编译期常量，不能直接定义静态二维数组。
所以用`vector`动态开辟二维数组。

### 等价分步写（方便看懂）

```
//先生成一行，n个false
vector<bool> oneRow(n, false);
//dp总共有n行，每一行复制oneRow
vector<vector<bool>> dp(n, oneRow);
```
———————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————
### 高频坑
1. 不要写反维度：`vector<vector<bool>> dp(n,vector<bool>(n,false))`
   - 第一个 n：**行数**
   - 第二个 n：**列数**
2. `vector<bool>`是特殊的位压缩容器，刷题没问题；如果想换普通版本：

```
vector<vector<char>> dp(n,vector<char>(n,0));
```
————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————
### 访问示例

```
dp[i][j] = true;      //把i行j列设为true
if(dp[i+1][j-1]){...} //读取i+1行，j‑1列的值
```
