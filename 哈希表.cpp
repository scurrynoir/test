| `unordered_map<K,V>` | 哈希表 |   平均 O (1)，**无序** |
| `map<K,V>`           | 红黑树 | O (logn)，自动从小到大排序 |
> 需要快查就用 `unordered_map`

### 基础语法示例
```
#include <unordered_map>
#include <iostream>
using namespace std;

int main()
{
    unordered_map<int,int> mp;

    //插入
    mp[2] = 0;
    mp[7] = 1;

    //查找key是否存在
    if(mp.find(2) != mp.end()){
        cout << mp[2]; //取value，输出0
    }

    //遍历
    for(auto& p : mp){
        //p.first 是key，p.second是value
        cout << p.first << "->" << p.second << endl;
    }

    return 0;
}
```
- `mp.find(key)`：找到返回迭代器；找不到返回 `mp.end()`
- `mp[key]`：
  - key 存在：读取 / 修改 value
  - key**不存在**：会自动插入这个 key！容易踩坑
——————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————
## 哈希冲突
两个不同 key 算出同一个数组下标。
解决两种主流方案：
1. **链地址法（拉链法）**：C++ 的`unordered_map`用这个
数组每个位置挂一条链表；冲突的元素全部挂在同一个链表上。
查找时先算下标，再遍历这条小链表。

2. 开放寻址法：冲突就往下找下一个空位。 >最坏情况全部冲突退化成链表，复杂度 O (n)，**平均还是 O (1)**。

—————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————
总结：

 操作                 数组                    链表               map (红黑树)                    unordered_map (哈希) 
 查找              O (1) 下标访问              O(n)                O(logn)                         平均 O (1) 
插入尾部               O(1)                    O(1)                O(logn)                         平均 O (1) 
 删除                  O(n)                   O(n)                 O(logn)                         平均 O (1) 
——————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————
###坑点
1. `unordered_map` **不排序**，输出顺序不一定是插入顺序
2. `mp[key]` 如果 key 不存在会新增键值对，判断存在一定要用`find`，不要直接 if (mp [key])
3. 可以存的 key：int、string；不能直接存 vector、自定义结构体做 key。
