###精简思维导图版总结
1. 访问控制：public、private、protected
2. 五大基础函数：构造、析构、拷贝构造、赋值重载、this 指针
3. static：类共享成员
4. friend：友元，访问私有
5. const‑成员函数不可修改属性
6. 运算符重载
7. 继承、访问权限
8. 虚函数、多态
9. 深浅拷贝、堆内存内存泄漏

—————————————————————————————————————————————————————————————————————## 一、类是什么——————————————————————————————————————————————————————————————————————————————————

类（class）相当于**模板 / 图纸**，对象就是根据图纸造出来的实物。
- 类：抽象模板，描述有什么变量 (属性)、什么函数 (行为)
- 对象：类实例化出来的实体，拥有独立内存

class Person
{
public:
    int age;      // 属性‑成员变量
    void sayHi()  // 行为‑成员函数
    {
        cout<<"你好"<<endl;
    }
};

int main()
{
    Person p;     // 根据模板创建对象p（实例化）
    p.age = 18;
    p.sayHi();
    return 0;
}
```

————————————————————————————————————————————————————————————## 二、三大访问权限（重点背诵）————————————————————————————————————————————————————————————————
1. **public（公有）**：类内、类外全都可以访问
2. **private（私有，默认权限）**：**只能在类内部访问**，外面碰不到
3. **protected（受保护）**：本类、子类可以访问，外部不行

> 开发规范：成员变量全部私有，对外提供 set 、get 函数读写

```
class Person
{
private:
    int age;       // 私有，外部不能直接 p.age
public:
    void setAge(int a)
    {
        if(a>0) age=a;
    }
    int getAge()
    {
        return age;
    }
};
```

————————————————————————————————————————————————————————————## 三、六大核心特殊成员函数————————————————————————————————————————————————————————————————————

### 1. 构造函数 Constructor
作用：**对象创建的时候自动调用，初始化成员**
规则：
1. 名字必须和类名一模一样
2. 没有返回值，连 void 都不要写
3. 可以重载（多个构造函数，参数不一样）
4. 初始化列表优先于函数体内赋值

```
class Point
{
public:
    int x,y;
    // 默认构造（无参）
    Point():x(0),y(0){}
    // 带参构造
    Point(int a,int b):x(a),y(b)}   //实现重载
};
```

### 2. 析构函数～Destructor
1. 名字：`~类名`
2. 无参数、不能重载、没有返回值
3. 对象销毁自动执行，专门用来释放堆内存 `delete[]`

```
class Arr
{
public:
    int* data;
    Arr(){data=new int[5];}
    ~Arr(){delete[] data;} // 释放堆空间，防止内存泄漏（只要对象生命周期结束、即将销毁，析构函数 `~Arr()` 一定会自动调用，不需要你手动调用，不受循环、函数、if‑else 约束）
};
```

### 3. 拷贝构造函数
格式：`类名(const 类名& 旧对象)`
创建新对象，用旧对象复制一份
- 默认拷贝构造做**浅拷贝**（指针只复制地址）
- 堆内存成员必须手写拷贝构造实现深拷贝

```
class Arr
{
public:
    int* data;
    Arr(){data=new int[3];}
    //拷贝构造，深拷贝
    Arr(const Arr& other)
    {
        data=new int[3];
        for(int i=0;i<3;i++)
            data[i]=other.data[i];
    }
    ~Arr(){delete[] data;}
};
```

### 4. 赋值运算符重载 operator=
用于 `a = b`，同样需要深拷贝、防止自赋值

```
Arr& operator=(const Arr& other)
{
    if(this == &other) return *this;
    delete[] data;    //此处把旧的内存释放掉
    data=new int[3];
    for(int i=0;i<3;i++) data[i]=other.data[i];
    return *this;
}
```

### 5. 移动构造、移动赋值（C++11，右值引用）
用来转移堆内存所有权，省去深拷贝，提升性能

——————————————————————————————————————————————————————————————————## 四、this‑指针———————————————————————————————————————————————————————————————————————
1. `this` 是成员函数隐藏参数，代表**当前调用该函数的对象地址**
2. 类型：`类名* const this`
3. 用处
   - 区分成员变量和局部变量重名
   - 返回对象自身 `return *this;`

```
class Person
{
public:
    int age;
    void setAge(int age)
    {
        this->age = age;
    }
};
```

————————————————————————————————————————————————————————————————————## 五、友元 friend——————————————————————————————————————————————————————————————————

> 打破封装，让外部函数 / 类可以访问本类私有成员

1. 友元函数
```
class Point
{
private:
    int x;
public:
    friend void print(Point p);
};
void print(Point p){cout<<p.x;}
```

2. 最常见：`operator<<` 输出重载必须全局友元（详见 友元 ）

————————————————————————————————————————————————————————————## 六、static 静态成员——————————————————————————————————————————————————————————————————————————

1. **静态成员变量**：不属于任何单个对象，全类共用一份内存，全局唯一
2. **静态成员函数**：没有 this 指针，只能访问静态变量，不能访问普通成员

```
class Person
{
public:
    static int count; // 记录创建多少个人
    Person(){count++;}
};
int Person::count = 0; // 类外初始化静态变量
```

————————————————————————————————————————————————————————————————## 七、const 修饰类相关————————————————————————————————————————————————————————————————————————

1. `const 成员函数`：函数内部**不能修改任何成员变量**
```
void show() const
{
    cout<<x;
}
```

2. const 对象只能调用 const 成员函数

——————————————————————————————————————————————————————————————————## 八、运算符重载——————————————————————————————————————————————————————————————————————————

1. 成员函数重载：`p1 + p2`
2. 流运算符 `<< >>` 只能全局友元重载，左边必须是 cout/cin
3. 前置 ++ 返回引用；后置 ++ 返回值副本、带 int 占位参数区分

————————————————————————————————————————————————————————————————## 九、继承（三大继承方式）————————————————————————————————————————————————————————————————

```
class 子类 : public 父类
```
1. public 继承：父 public→子 public、父 protected→子 protected1
2. protected 继承：父 public、protected 全部变成子类 protected
3. private 继承：父 public、protected 全部变成子类 private

### 继承重点
1. 子类构造会**先调用父类构造函数**
2. 析构顺序：先子类析构，后父类析构
3. 菱形继承、虚继承解决重复内存
4. 虚函数、多态

——————————————————————————————————————————————————————————————## 十、多态（C++ 面向对象核心）————————————————————————————————————————————————————————————————

1. 父类写虚函数 `virtual void func(){}`  （虚函数含义：允许子类重写这个函数；运行时根据真实对象类型，选择执行对应版本）
2. 子类重写该函数
3. 通过父类指针 / 父类引用调用，运行时自动识别执行子类函数

```
class Animal{
public:
    virtual void speak(){cout<<"动物叫声";}
};
class Dog:public Animal{
public:
    void speak() override{cout<<"汪汪";}
};

1. 重写 (覆盖) 父类的虚函数 speak
2. `override`：C++11 关键字，显式标明该函数用来重写父类虚函数
> 好处：一旦父类函数名字、参数写错，编译器直接报错提醒，防止重写失败
即： - 不加 override：只是靠程序员自觉保证重写成功，函数名写错就会变成全新函数、重写失败
     - 写上 override，编译器检查父类有没有一模一样的虚函数，写错直接报错

## 拓展：虚析构（高频坑点）
子类含有堆内存时，父类析构一定要写成虚析构
```
virtual ~Animal(){}
```
不然 `delete p;` 只会调用父类析构，子类析构不会执行，造成内存泄漏。


```

————————————————————————————————————————————————————————————## 十一、浅拷贝 vs 深拷贝——————————————————————————————————————————————————————————————————

1. **浅拷贝**：只是复制地址，两个对象共用同一块堆内存；析构时重复释放，程序崩溃
2. **深拷贝**：新开一块堆内存，复制内容，二者内存互相独立

————————————————————————————————————————————————————————————## 十二、初始化列表（必背）————————————————————————————————————————————————————————————————

下面几种成员**只能在初始化列表初始化**
1. 引用成员
2. const 常量成员
3. 没有默认构造函数的类成员

class Test
{
private:
    int& ref;
    const int num;
public:
    Test(int &r):ref(r),num(10){}
};
